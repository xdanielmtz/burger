<div class="text-center">
  <img src="https://i2.wp.com/freepngimages.com/wp-content/uploads/2016/11/bacon-burger.png?fit=895%2C895" alt="burger">
  <h1>Delicious Burgers!</h1>
</div>
<br>

<div class="container">
  <div class="row">
      <div class="col-sm-6">
          <h2>Burgers Not Eaten</h2>
              <ul>
                {{#each burgers}}
                  {{#unless eaten}}
                    {{> burgers/burger-block eat=true}}
                  {{/unless}}
                {{/each}}
              </ul>
      </div>
      <div class="col-sm-6">
          <h2>Burgers Devoured</h2>
          <ul>
            {{#each burgers}}
              {{#if eaten}}
                {{> burgers/burger-block eat=false}}
              {{/if}}
            {{/each}}
          </ul>
      </div>
  </div>
</div>

<br>

<div class="text-center">
  <h2>Add a Burger</h2>
  <br>
  <form class="create-form">

    <div class="form-group">
      <label for="bu">Burger Name:</label>
      <input type="text" id="bu" name="name">
    </div>

    <div class="form-group">
      <label for="eaten">Have You Eaten This Burger?</label><br>
      <input type="radio" name="eaten" value="1" checked> I Demolished That Burger<br>
      <input type="radio" name="eaten" value="0"> No, Not Yet
    </div>
    <br>
    <button class="btn btn-info" type="submit">Add</button>
  </form>
</div>
